# 图片压缩与预览功能 - 测试指南

## 功能概述

本次更新实现了两个主要功能：

### 1. 智能图片压缩
- **目标大小**：200KB - 300KB
- **压缩策略**：迭代调整质量和尺寸
- **特殊处理**：
  - 原图 < 200KB：直接上传，不放大
  - 原图在范围内：直接上传
  - 压缩失败：回退到原图

### 2. 图片预览功能
- **全屏预览**：点击图片打开全屏查看器
- **多图切换**：支持左右箭头、滑动手势切换
- **缩放功能**：点击图片可放大查看
- **缩略图条**：多图时显示底部缩略图

## 测试步骤

### 前置条件
1. 启动开发服务器：`npm run dev`
2. 在浏览器中打开 http://localhost:5173/
3. 登录应用（使用插件登录或私钥登录）
4. 在设置页面配置 Blossom 上传服务器（如 `https://blossom.primal.net`）

### 测试场景 1：大图压缩（>1MB）

**目的**：验证大图能被压缩到 200-300KB 范围

**步骤**：
1. 准备一张大于 1MB 的图片（例如：2MB、5MB 的高分辨率照片）
2. 点击主页的"发帖"按钮，打开发帖编辑器
3. 点击"上传图片"按钮，选择准备好的大图
4. 观察浏览器控制台（F12），查看压缩日志

**预期结果**：
```
开始压缩: example.jpg, 原始大小: 2048.5KB, 尺寸: 4000x3000
迭代 1: 质量=0.85, 缩放=1.00, 尺寸=4000x3000, 大小=850.2KB
迭代 2: 质量=0.75, 缩放=1.00, 尺寸=4000x3000, 大小=650.5KB
迭代 3: 质量=0.65, 缩放=1.00, 尺寸=4000x3000, 大小=480.3KB
迭代 4: 质量=0.55, 缩放=1.00, 尺寸=4000x3000, 大小=350.7KB
迭代 5: 质量=0.50, 缩放=0.90, 尺寸=3600x2700, 大小=280.4KB
✓ 压缩成功: 280.4KB 在目标范围内
压缩完成: 2048.5KB -> 280.4KB (13.7%), 迭代次数: 5
```

- 控制台显示压缩过程和最终结果
- 最终大小在 200-300KB 之间
- 图片能正常显示在预览区

### 测试场景 2：小图不放大（<200KB）

**目的**：验证小图不会被放大

**步骤**：
1. 准备一张小于 200KB 的图片（例如：100KB、150KB 的小图）
2. 点击"发帖"按钮，上传这张小图
3. 观察浏览器控制台

**预期结果**：
```
原图大小 150.5KB 已小于目标 200KB，无需压缩
```

- 控制台提示"无需压缩"
- 上传的文件大小与原图接近（加密后略大）
- 图片能正常显示

### 测试场景 3：中等大小图片（200KB-300KB）

**目的**：验证已在目标范围内的图片不会被压缩

**步骤**：
1. 准备一张 200-300KB 的图片
2. 上传该图片
3. 观察控制台

**预期结果**：
```
原图大小 250.3KB 已在目标范围内，无需压缩
```

- 直接上传，不进行压缩处理

### 测试场景 4：图片预览功能

**目的**：验证点击图片能打开全屏预览

**步骤**：
1. 在主页找到一条包含图片的帖子
2. 点击图片缩略图
3. 验证以下功能：
   - 全屏黑色背景显示
   - 图片清晰可见
   - 点击图片可以放大/缩小
   - 按 ESC 键关闭预览
   - 点击右上角 X 关闭预览

**预期结果**：
- 预览器以动画方式打开
- 图片居中显示
- 所有交互功能正常工作

### 测试场景 5：多图预览与切换

**目的**：验证多图切换功能

**步骤**：
1. 发布一条包含多张图片的帖子（3-5张）
2. 点击任意一张图片打开预览
3. 测试以下功能：
   - 点击左/右箭头切换图片
   - 按键盘 ← → 方向键切换
   - 在移动端左右滑动切换
   - 点击底部缩略图跳转到指定图片
   - 查看顶部图片计数器（如 "2 / 5"）

**预期结果**：
- 所有切换方式都能正常工作
- 图片切换流畅
- 计数器和缩略图高亮正确更新
- 第一张和最后一张时，对应箭头按钮变灰

### 测试场景 6：移动端测试

**目的**：验证移动端体验

**步骤**：
1. 在浏览器开发者工具中切换到移动设备模式（F12 → 切换设备工具栏）
2. 选择一个移动设备（如 iPhone 12）
3. 重复测试场景 1-5

**预期结果**：
- 压缩功能在移动端同样有效
- 触摸滑动能切换图片
- 按钮和界面元素适配移动屏幕
- 缩略图条在小屏幕上可滚动

### 测试场景 7：压缩失败兜底

**目的**：验证压缩失败时的兜底策略

**步骤**：
1. 可以通过上传非标准格式图片或损坏的图片文件来测试
2. 观察控制台和上传结果

**预期结果**：
```
图片压缩失败，返回原图: Error: ...
```

- 控制台显示错误但不中断上传
- 使用原图继续上传流程
- 用户收到提示但不影响使用

## 压缩算法说明

### 迭代压缩策略

```
初始参数：quality = 0.85, scale = 1.0

循环迭代（最多 10 次）：
1. 如果 size > maxTargetSize (300KB):
   - 如果 quality > 0.5: 降低 quality (-0.1)
   - 否则如果 scale > 0.5: 降低 scale (-0.1)
   - 否则: 同时降低 quality 和 scale (-0.05)
   
2. 如果 size 在 [200KB, 300KB]: 成功！
3. 如果 size < minTargetSize (200KB): 停止迭代
```

### 关键特性

- **高质量缩放**：使用 Canvas 的 `imageSmoothingQuality = "high"`
- **JPEG 输出**：所有压缩后的图片统一为 JPEG 格式
- **智能阈值**：压缩到略低于目标也接受（避免过度迭代）

## 常见问题

### Q1: 压缩后的图片质量如何？
A: 使用高质量缩放算法，在 200-300KB 范围内质量通常很好，足够清晰浏览和分享。

### Q2: 所有图片格式都支持吗？
A: 支持浏览器能识别的所有图片格式（JPG, PNG, GIF, WebP等），输出统一为 JPEG。

### Q3: 压缩会很慢吗？
A: 通常在 1-3 秒内完成，具体取决于原图大小和设备性能。

### Q4: 移动端性能如何？
A: 在现代移动设备上表现良好，使用浏览器原生 API，性能优化良好。

## 技术实现

### 核心文件

1. **`src/utils/imageCompression.ts`**
   - 迭代压缩算法
   - 目标大小控制
   - 异常处理

2. **`src/components/ImageViewer.vue`**
   - 全屏预览组件
   - 手势和键盘支持
   - 缩放和切换动画

3. **`src/components/PostEditorModal.vue`**
   - 集成压缩功能
   - 上传前预处理

4. **`src/components/PostImagePreview.vue`**
   - 点击触发预览
   - 传递图片数组

### 日志输出

所有压缩操作都会输出详细日志到控制台，格式：
```
开始压缩: 文件名, 原始大小, 尺寸
迭代 N: 质量, 缩放, 尺寸, 大小
✓ 压缩成功: 最终大小 在目标范围内
压缩完成: 原始 -> 最终 (压缩率%), 迭代次数
```

## 已知限制

1. **HEIC 格式**：浏览器对 HEIC 支持有限，建议用户转换为 JPG
2. **极大图片**：超过 20MB 的图片可能需要较长处理时间
3. **动图**：GIF 压缩后会变成静态图片

## 未来改进方向

- [ ] 支持保留动图（GIF/WebP）
- [ ] 添加压缩进度指示器
- [ ] 允许用户自定义目标大小
- [ ] 压缩前显示预估大小
