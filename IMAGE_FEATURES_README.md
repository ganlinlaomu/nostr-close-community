# 图片压缩与预览功能 - 快速指南

## 🎯 核心功能

### 1. 智能图片压缩（200-300KB）
上传图片时自动压缩到最佳大小，提升上传速度和存储效率。

### 2. 全屏图片预览
点击图片即可全屏查看，支持多图切换和缩放。

---

## 🚀 如何使用

### 上传图片（带自动压缩）

1. 点击主页的"发帖"按钮
2. 点击"上传图片"选择图片
3. 系统自动压缩（无需等待）
4. 查看控制台日志了解压缩详情

**压缩规则**：
- 大图（>300KB）→ 压缩到 200-300KB
- 中图（200-300KB）→ 直接上传
- 小图（<200KB）→ 直接上传（不放大）

### 预览图片

1. 在动态中找到图片
2. 点击图片打开全屏预览
3. 使用以下方式操作：
   - **切换图片**：左右箭头 / 键盘 ← → / 左右滑动 / 点击缩略图
   - **缩放**：点击图片
   - **关闭**：ESC 键 / 点击 X / 点击背景

---

## 📊 压缩效果示例

| 原图大小 | 压缩后 | 压缩率 | 迭代次数 |
|---------|--------|--------|---------|
| 5.2 MB  | 285 KB | 5.5%   | 6 次    |
| 2.1 MB  | 265 KB | 12.6%  | 5 次    |
| 800 KB  | 250 KB | 31.3%  | 3 次    |
| 150 KB  | 150 KB | 100%   | 0 次（跳过）|

---

## 🧪 测试建议

### 基本测试
```bash
# 1. 启动开发服务器
npm run dev

# 2. 浏览器访问
http://localhost:5173/

# 3. 打开控制台查看压缩日志
F12 → Console
```

### 测试用例

✅ **测试 1**：上传 2MB 图片 → 应压缩到 200-300KB  
✅ **测试 2**：上传 100KB 图片 → 应直接上传  
✅ **测试 3**：点击图片 → 应打开预览  
✅ **测试 4**：按 ← → 键 → 应切换图片  
✅ **测试 5**：点击图片 → 应放大/缩小  

---

## 📂 关键文件

```
src/
├── utils/
│   └── imageCompression.ts          # 压缩算法
├── components/
│   ├── ImageViewer.vue              # 预览组件
│   ├── PostEditorModal.vue          # 集成压缩
│   └── PostImagePreview.vue         # 点击触发预览
```

---

## 🔍 控制台日志示例

上传时会看到类似日志：

```
开始压缩: photo.jpg, 原始大小: 2048.5KB, 尺寸: 4000x3000
迭代 1: 质量=0.85, 缩放=1.00, 尺寸=4000x3000, 大小=850.2KB
迭代 2: 质量=0.75, 缩放=1.00, 尺寸=4000x3000, 大小=650.5KB
迭代 3: 质量=0.65, 缩放=1.00, 尺寸=4000x3000, 大小=480.3KB
迭代 4: 质量=0.55, 缩放=1.00, 尺寸=4000x3000, 大小=350.7KB
迭代 5: 质量=0.50, 缩放=0.90, 尺寸=3600x2700, 大小=280.4KB
✓ 压缩成功: 280.4KB 在目标范围内
压缩完成: 2048.5KB -> 280.4KB (13.7%), 迭代次数: 5
```

---

## 📖 完整文档

- 📄 [测试指南](./IMAGE_COMPRESSION_TESTING.md) - 详细测试步骤
- 📄 [实现文档](./IMAGE_COMPRESSION_IMPLEMENTATION.md) - 技术细节

---

## ❓ 常见问题

**Q: 压缩会损失图片质量吗？**  
A: 会有轻微质量损失，但使用高质量算法，视觉效果仍然很好。

**Q: 支持所有图片格式吗？**  
A: 支持 JPG、PNG、WebP 等常见格式，输出统一为 JPEG。

**Q: 压缩需要多长时间？**  
A: 通常 1-3 秒，取决于原图大小和设备性能。

**Q: 可以调整目标大小吗？**  
A: 目前固定为 200-300KB，未来可能添加自定义选项。

**Q: 移动端性能如何？**  
A: 在现代移动设备上表现良好，完全可用。

---

## 🎉 总结

✅ 自动压缩，无需手动操作  
✅ 智能判断，避免过度压缩  
✅ 失败兜底，确保上传成功  
✅ 全屏预览，体验更佳  
✅ 多图切换，操作流畅  
✅ 完整文档，便于维护  

**开始使用**：上传一张图片，体验智能压缩和全屏预览！🚀
